#include <gtest/gtest.h>

#include "sub.h"

TEST(SubTest, test1) { ASSERT_EQ(sub(1, 2), -1); }

TEST(SubTest, test2) { ASSERT_EQ(sub(1, 2), -1); }

TEST(SubTest, test3) { ASSERT_EQ(sub(1, 2), -1); }

TEST(SubTest, test4) { ASSERT_EQ(sub(1, 2), -1); }

TEST(SubTest, test5) { ASSERT_EQ(sub(1, 2), -1); }

TEST(SubTest, test6) { ASSERT_EQ(sub(1, 2), -1); }
